// Code your solutions in this file
// const cards = (["Jason", "liz", "Terry"]);

function writeCards(cards) {
    for(let i = 0; i < cards.length; i++) {
        console.log(`Thank you ${cards[i]} for the wonderful birthday cards`);
    }
    return cards;
}

    // writeCards(cards);
